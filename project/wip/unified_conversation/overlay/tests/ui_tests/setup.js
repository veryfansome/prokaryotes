// Overlay's conversation_client is a pure module with no DOM/markdown deps,
// so this setup is intentionally minimal. The full ui.js integration tests
// would need the upstream setup (markdown-it, DOMPurify, highlight.js globals).
